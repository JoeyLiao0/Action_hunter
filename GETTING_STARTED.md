# 🚀 快速开始指南

## 安装依赖

```bash
cd /Users/jiapeng.liao/Desktop/action_hunter
npm install
```

## 运行应用

### 开发模式（显示测试面板）

```bash
npm run dev
```

### 正常模式

```bash
npm start
```

## macOS 首次运行

首次运行时，系统会提示授予屏幕录制权限：

1. 点击"好"授予权限
2. 如果误点"不允许"，需要手动授权：
   - 打开"系统偏好设置"
   - 选择"安全性与隐私"
   - 点击"屏幕录制"
   - 勾选 "Electron" 或 "ActionHunter"
   - 重启应用

## 测试功能（开发模式）

运行 `npm run dev` 后，会看到测试面板，按顺序测试：

1. **🔐 测试权限检查**
   - 验证：显示权限状态，如果未授予会请求权限

2. **🖥️ 测试屏幕源列表**
   - 验证：列出所有屏幕和窗口

3. **📸 测试截图功能**
   - 验证：截取第一个屏幕，显示预览和文件大小

4. **🎬 测试录屏功能**
   - 验证：录制5秒视频，显示预览和文件大小

5. **📄 测试文档生成**
   - 验证：生成测试HTML文档，自动在浏览器打开

## 完整流程测试

1. **创建项目**
   - 输入项目名称："测试项目 - 用户登录流程"
   - 点击"开始项目"

2. **添加步骤**（至少10个）
   
   **截图步骤示例：**
   - 点击"📸 添加截图"
   - 选择屏幕或窗口
   - 输入说明："打开登录页面"
   - 确认

   **录屏步骤示例：**
   - 点击"🎬 录制视频 (15秒)"
   - 选择屏幕或窗口
   - 输入说明："输入用户名和密码"
   - 倒计时3秒后开始录制
   - 15秒自动停止

   **文本步骤示例：**
   - 点击"📝 添加文字说明"
   - 输入内容："注意：密码区分大小写"
   - 确认

3. **查看步骤列表**
   - 确认所有步骤已正确添加
   - 可以删除错误的步骤

4. **完成项目**
   - 点击"结束项目"
   - 确认完成

5. **生成文档**
   - 点击"📤 生成文档"
   - 选择保存位置
   - 在浏览器中查看生成的HTML文档

## 预期结果

生成的HTML文档应该：
- ✅ 包含项目名称和元数据
- ✅ 所有步骤按顺序显示
- ✅ 截图清晰可见
- ✅ 视频自动播放、循环
- ✅ 文本格式正确
- ✅ 单文件，包含所有资源
- ✅ 文件大小合理（10个步骤约25-35MB）

## 常见问题

### Q: 应用无法打开？
A: macOS首次运行需要右键点击 → "打开" → 确认

### Q: 截图/录屏失败？
A: 检查是否授予了屏幕录制权限

### Q: 视频无法播放？
A: 确保使用Chrome、Firefox或Safari等现代浏览器打开生成的HTML

### Q: 文件过大？
A: 
- 控制视频步骤数量
- 减少录制时长
- 使用截图代替部分视频

## 项目结构说明

```
action_hunter/
├── main.js                      # Electron主进程
├── preload.js                   # 安全桥接
├── package.json                 # 项目配置
├── src/
│   ├── platform/                # 平台抽象层
│   │   ├── interface.js         # 接口定义
│   │   ├── factory.js           # 平台工厂
│   │   ├── macos/               # macOS实现
│   │   │   └── ScreenCapture.js
│   │   └── windows/             # Windows预留
│   │       └── ScreenCapture.js
│   ├── core/                    # 业务逻辑
│   │   ├── ProjectManager.js    # 项目管理
│   │   └── DocumentGenerator.js # 文档生成
│   └── ui/                      # 界面层
│       ├── index.html           # 主界面
│       ├── renderer.js          # 渲染进程逻辑
│       └── styles.css           # 样式
└── node_modules/                # 依赖包
```

## 扩展到Windows

当前代码已预留Windows接口，添加Windows支持只需：

1. 在Windows机器上测试
2. 完善 `src/platform/windows/ScreenCapture.js`
3. 大部分代码可直接复用macOS实现

## 技术栈

- **Electron 28.0.0** - 桌面应用框架
- **原生JavaScript** - 无需额外打包工具
- **Electron API** - desktopCapturer, systemPreferences
- **Web API** - MediaRecorder, getUserMedia

## 开发模式特性

`npm run dev` 时会：
- 显示测试面板
- 打开开发者工具
- 显示详细日志

## 下一步

- [ ] 测试所有功能
- [ ] 录制实际操作文档
- [ ] 分享给团队使用
- [ ] 收集反馈改进

享受使用 ActionHunter！🎯
